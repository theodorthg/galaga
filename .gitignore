# Godot 4 — engine cache (regenerated on import; must NOT be committed)
.godot/
/android/
.import/

#export_presets.cfg

# CI / local export output
/build_ios/
/build_macos/
*.ipa
*.dmg
*.aab
*.apk
*.aar


# Downloaded Godot binary / templates (Codemagic fetches these each run)
/_godot/
/_tpl/
godot.zip
templates.tpz

# OS noise
.DS_Store

# Python bytecode cache (claude_transcript_to_html.py)
__pycache__/

# NOTE: the following ARE committed on purpose — Godot needs them:
#   *.import, *.uid, export_presets.cfg, project.godot, assets/**


# editor backup files
*~

# throwaway capture harness
/_capture.gd
/_capture.tscn
/_capture.gd.uid

# Claude Desktop auto-saves the live transcript here (session-UUID name); the
# deliberate end-of-project "Claude Code transcript.pdf" is still committable.
Claude Code transcript — *.pdf
Claude Code transcript*.html
